@model IEnumerable<Marca>

@{
    ViewData["Title"] = "Lista de Marca";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-xl">
    <h1 class="text-center my-4 fw-semibold">Lista de Marca</h1>

    <div class="d-flex justify-content-md-end mb-3">
        <a asp-action="Crear" class="btn btn-primary"> <i class="bi bi-plus-circle"></i> Nueva Marca</a>
    </div>

    <div class="table-responsive bg-white p-2 rounded-3 shadow-lg">
        <table id="tblDato" class="table table-striped w-100">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(b => b.Nombre)</th>
                    <th>@Html.DisplayNameFor(b => b.Estado)</th>
                    <th class="text-center">Acciones</th>
                </tr>
            </thead>
        </table>
    </div>
</div>

@section Scripts{
    <script src="~/js/marca.js"></script>
}

